#!/usr/bin/env ruby
require_relative "../config/boot"
require "bundler/audit/cli"

ARGV.concat %w[ check --update ] if ARGV.empty?
ARGV.concat %w[ --config config/bundler-audit.yml ] if ARGV.include?("check")
Bundler::Audit::CLI.start
